"""Generator configuration loading and defaults.

Stub (issue #1). Real implementation lands with the generator milestone:
loading the YAML config file, env-var-only secrets, and the configurable
annotation prefix (never hardcoded elsewhere).
"""
