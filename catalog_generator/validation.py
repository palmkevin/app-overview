"""Manifest validation rules — the single source of truth.

Stub (issue #1). All ``catalog-info.yaml`` validation rules live in this
module ONLY (also reused by the future Jenkins validation step). Do not
duplicate rule logic anywhere else.
"""
