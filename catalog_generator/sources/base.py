"""Abstract source interfaces implemented by both fake and real adapters.

Stub (issue #1). Interfaces land with the generator milestone: a repo
source (yields catalog-info.yaml manifests) and a runtime source (yields
observed Kubernetes deployment state).
"""
